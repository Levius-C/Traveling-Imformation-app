import React from "react";
import { Link } from "react-router-dom";

const Navbar = () => {
  return (
    <div>
      <Link to="/">HOME</Link>
      <input type="text" />
    </div>
  );
};

export default Navbar;
